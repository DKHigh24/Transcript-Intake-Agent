# Eval examples for the AI opportunity classifier
# Each line is a JSON object with "evidence", "expected", "source", "date_added"
# Minimum 5 examples required before --mode eval will run.
# Add examples here by running: --mode apply-feedback then --mode promote-feedback
# Or manually add labelled examples in the format below.
#
# Format:
# {"evidence": "<transcript chunk>", "expected": {"AIUseCaseType": "...", "ConfidenceLevel": "...", ...}, "source": "reviewer", "date_added": "YYYY-MM-DD"}
